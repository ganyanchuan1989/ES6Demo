doctype html
html
    head
        meta(charset="UTF-8")
        meta(http-equiv="Access-Control-Allow-Origin", content="*")
        title= htmlWebpackPlugin.options.title
       // script(type='text/javascript').
            var localHandler = function (data) {
                alert('我是本地函数，可以被跨域的remote.js文件调用，远程js带来的数据是：' + data.result);
            };
    body
        div#example
        div#header
            h1 这是标题

        div#main
            nav
                ul
                    li abc
                    li abc2
                    li abc3
        div#content
        div
            a(href="http://192.168.1.101:8080/hello.json") hello.json
        button#res 请求
        button#res21 请求sss
        button#res211 请求
        img(src="http://localhost:8090/tomcat-power.gif")